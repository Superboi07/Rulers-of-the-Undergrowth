using System.Collections;
using System.Collections.Generic;
using System;
using UnityEngine;

[CreateAssetMenu(fileName = "Data", menuName = "ScriptableObjects/DeckList", order = 2)]
public class SpawnDeckList : ScriptableObject
{
    // I have failed, thus I toil in repencence

    // teporary variables I will move later (if you see anything here, I forgot)

    // the decks
    public int[] SpindleDeck;
    public int[] AnnahDeck;
    public int[] TestDeck;

    // deck starting cards
    public int[] SpindleDeckStarter;
}